description = "Auto-derived from 707-char-struct-local-compound-add-obj (BCC fixture). cl /c /AS via the microsoft-msc5 oracle profile."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
