description = "for (i = 0; i != 10; i = i + 1) ; — probes for-loop shape against the while shape we already have."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
