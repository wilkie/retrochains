description = "while (x) x = x - 1; — simplest loop. Probes MSC backward jump shape and how x = x - 1 statement is parsed."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
