description = "8-case dense switch. -c parallel of 073. Probes BCC's jump-table dispatch: bounds-check via ja, scaled index via shl bx,1, indirect jmp through cs:<table>[bx], and a 16-byte jump table emitted in _TEXT with FIXUPPs to each case label."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
inputs = ["HELLO.C"]
asm_args = ["-S", "-ms", "HELLO.C"]
