description = "if (0) — probes whether MSC elides the dead body or keeps it (BCC keeps; we expect MSC to differ)."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
