description = "if (1) — probes whether MSC elides the unreachable trailing return (BCC elides)."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
