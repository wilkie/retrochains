description = "if (x) return 1; return 0; — simplest if. Probes how MSC tests a local for zero and how it numbers the skip-label."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
