description = "return *(p+1); — pointer arithmetic via *(ptr+K). Probes whether MSC lowers this identically to p[K] (fixture 4123) or via an explicit add+deref shape."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
