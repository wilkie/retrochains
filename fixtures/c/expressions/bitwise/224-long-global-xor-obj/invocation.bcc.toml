description = "`g = a ^ b;` long bitwise XOR. Same shape as fixtures 221/222: two independent per-half XOR ops, no carry."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
link_args = ['-LC:\LIB', '-ms', 'HELLO.C']
inputs = ["HELLO.C"]
asm_args = ["-S", "-ms", "HELLO.C"]
