description = "Return the sum of two literals (constant-folded by BCC to `mov ax,3`). Same source as 005-add-literals but -c."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
link_args = ['-LC:\LIB', '-ms', 'HELLO.C']
inputs = ["HELLO.C"]
asm_args = ["-S", "-ms", "HELLO.C"]
