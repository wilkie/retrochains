description = "printf(\"...\", g) where g is an init global. Probes pushing a global value as an argument (mov ax, [g]; push ax) vs the existing constant-push path."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
