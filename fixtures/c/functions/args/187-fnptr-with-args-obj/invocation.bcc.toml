description = "Function pointer with arguments: `int (*op)(int, int) = add; op(3, 4)`. Fixture 110 already covers the `void`-arg form; this tests that calls through `op` push the cdecl-style args before `call word ptr [bp-N]`."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
link_args = ['-LC:\LIB', '-ms', 'HELLO.C']
inputs = ["HELLO.C"]
asm_args = ["-S", "-ms", "HELLO.C"]
