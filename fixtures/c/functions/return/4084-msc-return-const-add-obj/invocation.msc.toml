description = "return 1 + 2; — probes whether MSC folds constant sub-expressions at parse time (BCC does)."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
