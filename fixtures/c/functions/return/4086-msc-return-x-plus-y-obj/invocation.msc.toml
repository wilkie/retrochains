description = "return x + y; — two uninitialized locals. Exercises register pool when both operands are non-const."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
