description = "return 1; — first non-zero return literal under MSC. Smallest possible immediate change vs the empty-main baseline."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
