description = "static-arr-compound-add-si: arr[i] compound-modify then read reuses element addr in SI"
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
