description = "char s[3] = \"hi\"; — char array initialized with a string literal. The bytes land directly in _DATA (no CONST routing, unlike `char *p = \"hi\";`)."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
