description = "`a <<= 1;` for long. Probes whether the K=1 case stays inlined (shl/rcl) or goes through helper."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
inputs = ["HELLO.C"]
asm_args = ["-S", "-ms", "HELLO.C"]
