description = "Auto-derived from 1661-return-cmp-result-direct-obj (BCC fixture). cl /c /AS via the microsoft-msc5 oracle profile."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
