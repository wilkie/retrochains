description = "Empty main(), compile to OBJ with MSC 5.0. First MSC oracle smoke test — confirms the microsoft-msc5 profile drives CL.EXE end-to-end."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
link_args = ['/AS', 'HELLO.C']
inputs = ["HELLO.C"]
