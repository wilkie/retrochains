description = "no-default switch: case1 nested-switch(jump-out), case2 fall-through (continuation lands here, mid-chain), case3 return after RL"
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
