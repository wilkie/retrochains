description = "First printf call. Probes external EXTDEF for printf, FIXUP from the call site, and how MSC pools the format string in CONST."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
