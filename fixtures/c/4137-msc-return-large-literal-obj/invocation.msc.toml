description = "return 1000; — int literal that does NOT fit in i8 sign-extended. Confirms MSC's mov ax, imm16 path; companion to 4077 (small value)."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
