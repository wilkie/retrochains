description = "While loop with FOUR modified locals — more than SI+DI together. Tells us which locals get the available registers and which stay on the stack, and in what priority order."
tool = "bcc"
args = ["-S", "-ms", "HELLO.C"]
inputs = ["HELLO.C"]
