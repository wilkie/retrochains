description = "`g = a >> 2;` signed long right-shift by two. Probe: confirms BCC calls a runtime helper (presumably `N_LXRSH@`) for K>1."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
inputs = ["HELLO.C"]
