description = "*q = 5; — store an int literal through a pointer global. Probes the load-pointer-then-store shape MSC picks for a write through an aliased target."
tool = "cl"
args = ["/c", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
