description = "Right-shift of an unsigned int. Should emit `shr` (logical) instead of `sar` (arithmetic, fixture 018). The shift-count loaded into CL as usual."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
inputs = ["HELLO.C"]
