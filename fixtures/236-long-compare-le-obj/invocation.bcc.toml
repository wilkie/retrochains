description = "`if (a <= b)` for long globals. Verifies the `<=` mnemonic mapping (jg→false, jl→true, ja→false — strict-less low half becomes non-strict)."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
inputs = ["HELLO.C"]
asm_args = ["-S", "-ms", "HELLO.C"]
