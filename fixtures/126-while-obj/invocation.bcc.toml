description = "Simple while loop incrementing to 10. -c parallel of 027. Exercises backward Jcc, push/pop si, xor si,si, inc si (1-byte form), cmp si,imm8."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
inputs = ["HELLO.C"]
