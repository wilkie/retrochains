description = "Standalone `<` comparison returning the resulting 0/1 value. Discover BCC's comparison-as-value materialization pattern."
tool = "bcc"
args = ["-S", "-ms", "HELLO.C"]
inputs = ["HELLO.C"]
