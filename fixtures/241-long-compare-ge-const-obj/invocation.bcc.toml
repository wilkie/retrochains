description = "`if (g >= 5)` for long. Cross-checks slice 240's mnemonic table for the non-strict (`>=`) case: should use jne instead of signed-jcc on the high-half true jump."
tool = "bcc"
args = ["-c", "-ms", "HELLO.C"]
inputs = ["HELLO.C"]
asm_args = ["-S", "-ms", "HELLO.C"]
