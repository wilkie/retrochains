description = "Auto-derived from 1207-nested-for-loop-counter-obj (BCC fixture). cl /c /AS via the microsoft-msc5 oracle profile."
tool = "cl"
args = ["/c", "/Fa", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
