description = "return 'A'; — char literal as an int expression. Probes single-quoted-char tokenization; the literal widens to int and lowers like `return 65;`."
tool = "cl"
args = ["/c", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
