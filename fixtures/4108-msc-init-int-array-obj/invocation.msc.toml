description = "int a[3] = {1,2,3}; — initializer list lays 3 ints into _DATA in source order. Probes brace-list parsing and 6-byte LEDATA payload."
tool = "cl"
args = ["/c", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
