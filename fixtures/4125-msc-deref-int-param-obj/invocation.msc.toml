description = "int get(int *p){return *p;} — dereference a pointer parameter (frame-relative load + indirect word read). Different from 4111 which derefs a global ptr."
tool = "cl"
args = ["/c", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
