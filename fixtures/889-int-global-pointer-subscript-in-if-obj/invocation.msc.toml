description = "Auto-derived from 889-int-global-pointer-subscript-in-if-obj (BCC fixture). cl /c /AS via the microsoft-msc5 oracle profile."
tool = "cl"
args = ["/c", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
