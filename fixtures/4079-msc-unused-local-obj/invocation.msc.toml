description = "Single int local declared, never used. Probes whether MSC reserves a frame slot for the unused declaration or elides it."
tool = "cl"
args = ["/c", "/AS", "HELLO.C"]
inputs = ["HELLO.C"]
