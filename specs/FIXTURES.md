# Fixtures and the capture/verify harness

The reverse-engineering work is organized around a **growing corpus of
fixtures**. Each fixture is a small input + an exact recorded oracle output
that we want our toolchain to reproduce. The collection is the spec for
"what we can currently compile correctly."

## Directory layout

Fixtures live under `fixtures/`, organized **by source language first**, then
**by feature area and sub-feature**. The harness discovers fixtures
**recursively** (`fixtures::discover_fixtures`) — a fixture is any directory
containing an `invocation.<compiler>.toml`, at any depth — so the tree can
grow new languages and categories freely:

```
fixtures/
├── c/                          # C fixtures (Borland C++ 2.0 / MSC 5.0 as C)
│   ├── expressions/            # feature AREA
│   │   ├── arithmetic/         #   SUB-FEATURE
│   │   │   ├── 005-add-literals/
│   │   │   │   ├── invocation.bcc.toml  # how to invoke each oracle / our toolchain
│   │   │   │   ├── invocation.msc.toml  # (a fixture may target multiple compilers)
│   │   │   │   ├── HELLO.C              # the source(s); DOS 8.3 uppercase names
│   │   │   │   └── expected/            # TRACKED manifests, one subdir per family
│   │   │   │       ├── bcc/BC2.toml     #   the sha256 contract, release-keyed
│   │   │   │       └── msc/MSC500.toml
│   │   │   └── 1184-int-add-three-distinct/
│   │   │   # The golden bytes (HELLO.OBJ/.ASM + stdout/stderr) are a GITIGNORED
│   │   │   # cache under artifacts/<same-path>/<family>/<release>/, regen via
│   │   │   # `xfix materialize`. See artifacts/README.md.
│   │   ├── bitwise/  compound-assign/  compare/  cast/  ...
│   ├── control-flow/           # switch/  loops/  conditionals/  jumps/
│   ├── aggregates/             # struct/  union/  bitfields/
│   ├── arrays/  pointers/  functions/  types/  floating-point/
│   ├── library/  intrinsics/  preprocessor/  codegen/
│   └── misc/unsorted/          # holding bucket: not yet categorized
└── cpp/                        # (future) C++ fixtures — e.g. cpp/expressions/...
```

Conventions:
- **Language dir** (`c/`, `cpp/`, …) is the top level. Wildly different
  languages get their own subtree.
- **Every fixture is categorized** into `fixtures/<lang>/<area>/<sub>/<NNN-slug>/`.
  Areas are the feature space (expressions, control-flow, aggregates, arrays,
  pointers, functions, types, floating-point, library, intrinsics,
  preprocessor, codegen); each splits into sub-features. The nesting makes
  **coverage gaps visible** — you can see at a glance which (area, sub) cells
  are thin. `codegen/` holds discriminators about *how* code is emitted
  (register-allocation, symbol-ordering) rather than a language feature.
- **Global numbering per language.** Each fixture keeps a unique sequential
  number (the `NNN-` prefix) that is stable across the whole language corpus
  regardless of which folder it lives in. Moving a fixture between categories
  **keeps its number**; code comments cite the number ("fixture 4148"), and
  `mscdiff.sh` / `xfix` resolve a fixture by bare name or number at any depth.
- **Assigning a category** is rule-based from the descriptive slug via
  `scripts/categorize_fixtures.py` (an ordered `RULES` table, most-specific
  construct first). Re-run it to propose homes for new fixtures or to audit
  the current layout; refine the rules rather than hand-sorting in bulk.
- New oracle-captured fixtures continue the global numbering and land in the
  area/sub their slug implies (or `misc/unsorted/` pending classification).

- **Source files** live at the fixture root, named as the tool will see
  them inside the DOS work directory. DOS is 8.3 and case-insensitive, so
  uppercase names with at-most-eight-character bases are conventional.
- **`invocation.toml`** is the recipe (see schema below).
- **`expected/`** is generated by `xfix capture`. The **contract** — the
  `sha256` and `size` of every output — lives in the tracked, release-keyed
  manifest `expected/<family>/<RELEASE>.toml` (e.g. `expected/bcc/BC2.toml`);
  our toolchain must reproduce those hashes byte-for-byte. The **bytes
  themselves** — the binary outputs (`.OBJ`, `.ASM`, `.EXE`, `.MAP`) plus the
  captured `stdout`/`stderr` — are NOT tracked: they are a reproducible cache,
  gitignored under `artifacts/` (which mirrors the fixtures tree, then keys by
  family/release: `artifacts/<fixture-path>/<family>/<RELEASE>/`), so git prunes
  the whole cache in one step. Regenerate them on demand with `xfix materialize
  <fixture>`, which re-drives the oracle and refuses to write unless each
  reproduction matches the recorded hash (so a fresh checkout both rebuilds the
  cache and *proves* the goldens still reproduce). Only the release-keyed
  manifest stays tracked under `expected/`. See `artifacts/README.md`.

## `invocation.toml` schema

```toml
# Which oracle tool to invoke. One of: "bcc", "tasm", "tlink".
tool = "bcc"

# Arguments passed verbatim. Filenames here must match files in the
# fixture root (case-sensitive on the host, uppercase by convention).
args = ["-S", "-ms", "HELLO.C"]

# Which files in the fixture root to materialize into the DOS work
# directory. Omit to default to "every non-expected/ file at the root".
# inputs = ["HELLO.C"]

# Optional human description, kept in git for context.
description = "Empty main, compile to assembly only"
```

## Two modes, one harness

The harness exposes two symmetric modes. Both load the same fixture and
both produce the same kind of comparison report; they differ only in which
toolchain they drive.

### `xfix capture <fixture>`

Runs the **oracle** (`crates/oracle/`) against the fixture. Writes the
result into `expected/`. Used when:

- Adding a brand-new fixture.
- Intentionally accepting a change (e.g. you fixed a fixture's input to be
  more representative).

Running `capture` overwrites `expected/` for that fixture. Review the diff
before committing.

### `xfix verify --toolchain T <fixture>`

Runs a toolchain against the fixture and diffs the result against
`expected/`. Used by `cargo test`, CI, and local development.

`T` is one of:

- **`oracle`** (default): re-runs the oracle. This must be a no-op — the
  oracle is deterministic by construction, so the diff must be empty. Use
  it as a self-check on the harness and the goldens themselves.
- **`ours`**: runs the corresponding host binary from `target/debug/`
  (currently `bcc`; later `tasm`, `tlink`). This is the path our
  reimplementation tests run against.

### Gating vs. advisory differences

`verify` reports two tiers of difference:

- **Gating**: the exit code, the list of output files, and each output's
  size, sha256, and mtime — all read from the tracked `<RELEASE>.toml` manifest. A
  gating mismatch fails verify and exits non-zero. The byte-exact contract is
  the recorded **sha256**, so verification gates correctly whether or not the
  cached golden bytes are present on disk. When they *are* present, verify
  additionally renders a byte-window diff to pinpoint where output diverged.
- **Advisory**: stdout and stderr bytes (and their sha256s in the
  manifest). Reported but not fail-on-mismatch under `--toolchain ours`.

The reason: BCC's stdout includes lines like
`\tAvailable memory 426624\r\r\n` — the host DOS RAM count reported by
the BC2 tool, observed under DOSBox. A native Rust reimplementation runs
outside DOS and can't reproduce this number. Treating streams as advisory
means our compiler can be byte-exact in what *matters* (the compilation
product) without us having to mimic DOS-emulator-specific banners.

Under `--toolchain oracle` everything is gating — there's no excuse for
the oracle to drift from itself.

## Diff reporting

The current harness reports two kinds of differences:

1. **Manifest diffs** for exit code, output-file presence, output-file
   size, SHA-256, mtime, and stream SHA-256.
2. **Byte diffs** for file contents: either a length mismatch or a
   16-byte hex window centered on the first differing byte. These appear
   only when the cached golden bytes are present; run `xfix materialize
   <fixture>` first on a fresh checkout to repopulate them.

Format-aware OMF/ASM/MZ diffs and text unified diffs are desirable future
improvements, but they are not implemented in `xfix` today. Until they
land, fixture failures should be investigated with the raw byte window, the
materialized `expected/` goldens, and the format readers in `crates/obj/`
or the ad-hoc fingerprinting tools.

## Adding a fixture (workflow)

1. Pick a feature you want our toolchain to handle next (or a bug to
   pin down).
2. Write the smallest C / asm input that exercises it. Drop it under
   `fixtures/NNN-short-name/` with an `invocation.toml`.
3. `xfix capture fixtures/NNN-short-name/` — produces the tracked release-keyed
   manifest under `expected/<family>/<RELEASE>.toml` plus the gitignored golden
   bytes (incl. stdout/stderr) under `artifacts/`.
4. Inspect the manifest / artifacts to convince yourself it's what you wanted.
5. `git add fixtures/NNN-short-name/` — commit. Only the `<RELEASE>.toml`
   manifest (the sha256 contract) is tracked; the `.OBJ`/`.ASM` bytes and
   captured `stdout`/`stderr` are the gitignored `artifacts/` cache and stay
   local. The recorded hashes are now the contract.
6. `xfix verify --toolchain ours fixtures/NNN-short-name/` — will fail
   until our toolchain catches up.
7. Implement what's needed in our toolchain. Re-run `verify`. Iterate
   until it matches.

## Determinism

Both modes inherit the oracle's clock-pinning (`OracleConfig::fake_time`,
default 1991-04-23 12:00:00 UTC), so the captured goldens don't drift
between machines or runs. See [`RUNNING_BCC.md`](RUNNING_BCC.md) for the
mechanism — there are now **three** moving parts:

1. **`faketime`** wraps DOSBox to fake the libc time calls.
2. **Input file mtimes** are stamped to the pin before the tool runs (BCC
   reads them via DOS stat and embeds them in OMF output).
3. **Output file mtimes** are normalized to the pin *after* the tool runs.
   The host kernel stamps every file the tool writes with the host's
   wall-clock time — that path bypasses faketime entirely. Without
   normalization, the manifest's `mtime` field would drift between every
   run.

If you find a *new* source of nondeterminism — output that varies between
two runs of the same fixture against the oracle — that's a bug in the
oracle wrapper, not the fixture. Fix the oracle.

## Why `stderr` is always empty

`OracleRun::stderr` and the cached `stderr` exist for forward-compatibility,
but under DOSBox 0.74 they're always empty. DOSBox's shell honors `cmd …
2>FILE` as a redirect *but also leaves the leading `2` in the command's
argv*, so adding stderr capture breaks every tool we run with it. We
combine into stdout instead; Borland tools write almost everything there
anyway.
